function submitForm(x) {
    // console.log(x.parentElement.getElementsByTagName('input')[0].value);
    // x.parentElement.submit();//.getElementsByTagName("input")[0].value);
    // //this.parentElement.submit();
    x.submit();
}